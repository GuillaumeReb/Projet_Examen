{% extends 'base.html.twig' %}

{% block title %}Typebiere index{% endblock %}

{% block body %}
<main class="container">
    <section class="row">
        <div class="col-12">
            <br>
            <h1 style="text-align:center">Types de bière</h1>
            <br>
            <a class="btn btn-primary" href="{{ path('app_typebiere_new') }}">{{ 'action.create'|trans }}</a>
            <br>
            <br>
            <table class="table table-striped table-hover table-bordered align-middle">
                <thead class="table-warning">
                    <tr>
                        <th style="text-align:center">{{ 'label.id'|trans }}</th>
                        <th style="text-align:center">{{ 'label.name'|trans }}</th>
                        <th style="text-align:center">{{ 'label.actions'|trans }}</th>
                    </tr>
                </thead>
                <tbody>
                    {% for typebiere in pagination.items %}
                        <tr class="table-light">
                            <td style="text-align:center">{{ typebiere.id }}</td>
                            <td style="text-align:center">{{ typebiere.name }}</td>
                            <td style="text-align:center">
                                <a class="btn btn-success" href="{{ path('app_typebiere_edit', {'id': typebiere.id}) }}">{{ 'action.update'|trans }}</a>
                                <a class="btn btn-danger" href="{{ path('app_typebiere_show', {'id': typebiere.id}) }}">{{ 'action.delete'|trans }}</a>
                            </td>
                        </tr>
                    {% else %}
                        <tr>
                            <td colspan="3">No records found</td>
                        </tr>
                    {% endfor %}
                </tbody>
            </table>
            <div class="d-flex justify-content-center mt-4">
                {{ knp_pagination_render(pagination) }}
            </div>
        </div>
    </section>
    <br>
</main>
{% endblock %}

